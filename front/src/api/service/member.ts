// 로그인 관련
import api from "../instances/api";

// 회원가입
// export const createUser = (email: string, password: string, nickname: string) =>
//   api.post(`member/signup`, {
// email,
// password,
// nickname,
//   });

// 로그인
// export const loginUser = (email:string, password:string) =>
// api.post(`member/login`, {email, password})

// 게시글 공유
// export const shareNote = (userId: number, document:number) =>
// api.put(`member/share`, {userId, document})
